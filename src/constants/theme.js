import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {},
  typography: {},
  props: {
    MuiButtonBase: {
      // disableRipple: true,
    },
  },
  overrides: {},
});

export default theme;
